header.bsk-page-header.app-sample-header
  if sample.code
    h1 #[strong ##{ sample.sample_number }] #{ sample.title } #[small #[code #{ sample.code }]] #[small #[a(href='/' + testbed_version + '/') Back to index]]
  else
    h1 #[strong ##{ sample.sample_number }] #{ sample.title } #[small #[a(href='/' + testbed_version + '/') Back to index]]

  include ./header--breakpoint.pug
