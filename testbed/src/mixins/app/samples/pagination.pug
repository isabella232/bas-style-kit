mixin pagination(items, variant, variant_item)
  if items == 1
    nav
      ul.bsk-pagination(class!=attributes.class)
        if variant_item == 1
          if variant == 'active'
            li.bsk-active: a(href='#') 1
          if variant == 'disabled'
            li.bsk-disabled: a(href='#') 1
        else
          li: a(href='#') 1
  else if items == 2
    nav
      ul.bsk-pagination(class!=attributes.class)
        li: a(href='#') First
        li: a(href='#') Last
  else if items > 2
    nav
      ul.bsk-pagination(class!=attributes.class)
        li: a(href='#') First
        each item in [...Array(items - 2).keys()]
          if item == (variant_item - 1)
            if variant == 'active'
              li.bsk-active: a(href="#") #{ item + 1 }
            if variant == 'disabled'
              li.bsk-disabled: a(href="#") #{ item + 1 }
          else
            li: a(href="#") #{ item + 1 }
        li: a(href='#') Last

mixin pager(previous, next, previous_disabled, next_disabled)
  nav
    ul.bsk-pager(class!=attributes.class)
      if previous == 'yes'
        if previous_disabled == 'yes'
          li.bsk-disabled: a(href='#') Previous
        else
          li: a(href='#') Previous
      if next == 'yes'
        if next_disabled == 'yes'
          li.bsk-disabled: a(href='#') Next
        else
          li: a(href='#') Next
