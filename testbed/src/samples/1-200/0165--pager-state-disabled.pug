//-
  sample:
    title: Disabled pager state
    code: .bsk-pager .bsk-disabled
  collections:
    - components--pagination
  ---

extends ../layouts/app-sample.pug
include ../mixins/app/samples/pagination.pug

block sample
  section.app-sample-section
    header Single item (next)
    +pager('no', 'yes', 'no', 'yes')

  section.app-sample-section
    header Single item (previous)
    +pager('yes', 'no', 'yes', 'no')

  section.app-sample-section
    header Both items
    +pager('yes', 'yes', 'yes', 'yes')
